# Тестовое задание

Внешние пакеты находятся в файле requirements.txt
Тесты запускаются через командой "pytest -v" (-v для более детальной информации)
Или с помощью контейнера
docker build -t test_task .
docker run test_task
